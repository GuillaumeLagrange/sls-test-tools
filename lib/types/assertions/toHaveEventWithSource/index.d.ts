import { TestResultOutput } from "../../utils/testResult";
declare const _default: {
    toHaveEventWithSource({ Messages }: {
        Messages: [{
            Body: string;
        }];
    }, expectedSourceName: string): TestResultOutput;
};
export default _default;
